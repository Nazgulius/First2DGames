
using UnityEngine;

public class FinishLevel : MonoBehaviour {

	public GameObject finish;

	void OnTriggerEnter2D (Collider2D other){
		if (other.tag == "Player"){
			other.transform.position = finish.transform.position; 	
		}
  	}
}
